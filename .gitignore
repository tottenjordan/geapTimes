# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# uv / virtualenv
.venv/
# NOTE: uv.lock IS committed (geapTimes is an application — reproducible installs).

# Environment / secrets
.env
.env.*
*.key
# NOTE: .secrets.baseline is intentionally committed — it is the detect-secrets audit
# baseline (a manifest of reviewed non-secrets), not a secret itself.

# Tooling caches
.ruff_cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.ty_cache/
.ipynb_checkpoints/

# OS / editor
.DS_Store
*.swp
